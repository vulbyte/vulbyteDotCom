export default function HS256(){
	let h = [2,3,5,7,11,13,17,19];
	//square each
	for(let i = 0; i < h.length; ++i){h[i] = h[i] * h[i];}
	//if imma do all this imma just use a lattice based encryption to future proof lolz	
}
